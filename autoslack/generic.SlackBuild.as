include(autoslack.m4)

AS_HEADER(PRGNAM, VERSION, AUTHOR, EMAIL)

AS_VARS(PRGNAM, VERSION)

AS_ARCH

set -e

AS_DOWNLOAD(DOWNLOAD)

AS_PREPARE(TAREXT)

AS_CONFIGURE

make
make install DESTDIR=$PKG

AS_STRIP

AS_COPY_DOCS

AS_MAKEPKG